SELECT DISTINCT taxavernaculars."VernacularName", users."uid", taxa."RankId", tmtraits."traitid"
FROM taxa 
LEFT JOIN taxavernaculars ON taxavernaculars."TID" = taxa."TID"
LEFT JOIN tmtraittaxalink ON taxa."TID" = tmtraittaxalink."tid"
LEFT JOIN tmtraits ON tmtraittaxalink."traitid" = tmtraits."traitid"
LEFT JOIN users ON taxa."modifiedUid" = users."uid"
ORDER BY taxavernaculars."VernacularName", users."uid", taxa."RankId", tmtraits."traitid"!!!tmtraittaxalink,tmtraits,traitid@taxavernaculars,taxa,TID@taxa,tmtraittaxalink,TID@taxa,users,modifiedUid@!!!4317!!!SELECT DISTINCT taxavernaculars."VernacularName", users."uid", taxa."RankId", tmtraits."traitid"
FROM taxa 
LEFT JOIN taxavernaculars ON taxavernaculars."TID" = taxa."TID"
LEFT JOIN users ON taxa."modifiedUid" = users."uid"
LEFT JOIN tmtraits ON users."uid" = tmtraits."modifieduid"
ORDER BY taxavernaculars."VernacularName", users."uid", taxa."RankId", tmtraits."traitid"!!!taxavernaculars,taxa,TID@taxa,users,modifiedUid@users,tmtraits,uid@!!!70!!!SELECT DISTINCT taxavernaculars."VernacularName", users."uid", taxa."RankId", tmtraits."traitid"
FROM taxa 
LEFT JOIN taxavernaculars ON taxavernaculars."TID" = taxa."TID"
LEFT JOIN users ON taxa."modifiedUid" = users."uid"
LEFT JOIN tmtraits ON users."uid" = tmtraits."createduid"
ORDER BY taxavernaculars."VernacularName", users."uid", taxa."RankId", tmtraits."traitid"!!!taxavernaculars,taxa,TID@users,tmtraits,uid@taxa,users,modifiedUid@!!!70!!!